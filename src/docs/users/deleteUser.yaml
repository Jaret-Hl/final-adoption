paths:
  /api/users/{pid}:
    delete:
      summary: Eliminar usuario
      description: Delete a user
      tags:
        - Users
      parameters:
        - in: path
          name: pid
          required: true
          description: Id user
          schema:
            type: string
      responses:
        200:
          description: Delete user

